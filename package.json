{
  "name": "social-media-blocker-260703",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Austere Chrome extension that blocks social media and video streaming sites beside a full-height outdoor photo until a reason is recorded.",
  "scripts": {
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "sync:manifest-version": "node scripts/sync-manifest-version.mjs",
    "build": "npm run clean && npm run sync:manifest-version && tsc -p tsconfig.json && node scripts/copy-static.mjs",
    "package:chrome": "node scripts/package-chrome-store.mjs"
  },
  "devDependencies": {
    "@types/chrome": "latest",
    "typescript": "latest"
  }
}
